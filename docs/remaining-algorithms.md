# Remaining Non-Duplicate Algorithms

Bu liste, `duplicate-algorithms.md` icindeki cok kaynakli algoritmalar ayiklandiktan sonra kalan algoritmalari icerir. Algopit'te tamamlanmis detay ekranlari da bu calisma setinden dusulmustur.

Kaynak kisaltmalari:

- `Sedgewick`: `ALGORITHMS - ROBERT SEDGEWICK.pdf`
- `DAA Notes`: `Design and Analysis Algorithms.pdf`
- `DSA`: `Dsa.pdf`
- `Aho`: `dS&A Book By Alfred -Aho.pdf`

## Matematik / Sayisal

- Primality test (`DSA`)
- Base conversion / ToBinary (`DSA`)
- Factorial (`DSA`)
- Exponentiation (`Sedgewick`)
- Polynomial evaluation (`Sedgewick`)
- Polynomial interpolation / Lagrange interpolation (`Sedgewick`)
- Polynomial multiplication (`Sedgewick`)
- Sparse polynomial addition (`Sedgewick`)
- Matrix addition (`Sedgewick`)
- Matrix-vector multiplication (`Sedgewick`)
- Gaussian elimination (`Sedgewick`)
- Gauss-Jordan method (`Sedgewick`)
- Forward elimination (`Sedgewick`)
- Backward substitution (`Sedgewick`)
- Spline interpolation (`Sedgewick`)
- Least-squares fitting (`Sedgewick`)
- Rectangle quadrature (`Sedgewick`)
- Trapezoid method (`Sedgewick`)
- Simpson's method (`Sedgewick`)
- Romberg integration (`Sedgewick`)
- Adaptive quadrature (`Sedgewick`)
- Spline quadrature (`Sedgewick`)
- Symbolic integration (`Sedgewick`)
- Linear congruential generator (`Sedgewick`)
- Additive congruential generator (`Sedgewick`)
- Chi-square randomness test (`Sedgewick`)
- Linear feedback shift register (`Sedgewick`)

## Sorting / Selection / Merging

- Randomized quicksort (`DAA Notes`)
- Partition / pivot partitioning (`DAA Notes`)
- Median-of-three partitioning (`Sedgewick`)
- Radix exchange sort (`Sedgewick`)
- Straight radix sort (`Sedgewick`)
- Bin sorting (`Aho`)
- Linear sort (`Sedgewick`)
- Sort3 / sorting three elements (`Sedgewick`)
- Kth element / order statistics (`Aho`)
- Worst-case linear kth element (`Aho`)
- Sort-merge (`Sedgewick`)
- Balanced multiway merging (`Sedgewick`)
- Replacement selection (`Sedgewick`)

## Searching / Hashing / Trees

- Sequential search (`DSA`)
- Probability search (`DSA`)
- Self-organizing search (`Sedgewick`)
- Recursive binary search (`DAA Notes`)
- Iterative binary search (`DAA Notes`)
- Interpolation search (`Sedgewick`)
- Binary tree search (`Sedgewick`)
- Red-black tree insertion (`Sedgewick`)
- Top-down 2-3-4 tree algorithms (`Sedgewick`)
- 2-3 tree insertion/deletion (`Aho`)
- Digital search tree search/insertion (`Sedgewick`)
- Patricia search/insertion (`Sedgewick`)
- Multiway radix searching (`Sedgewick`)
- Indexed sequential access (`Sedgewick`)
- External searching (`Sedgewick`)
- Extendible hashing (`Sedgewick`)
- Linear probing (`Sedgewick`)
- Double hashing (`Sedgewick`)
- Closed hash table dictionary operations (`Aho`)
- Open hash table dictionary operations (`Aho`)
- Hash bucket chaining (`Aho`)

## Heap / Priority Queue / Set / Union-Find

- Heap construction (`Sedgewick`)
- Indirect heap algorithms (`Sedgewick`)
- Simple union (`DAA Notes`)
- Simple find (`DAA Notes`)
- Quick union (`Sedgewick`)
- Union-find with weight balancing (`Sedgewick`)
- Union-find with height balancing (`Sedgewick`)
- Collapsing find (`DAA Notes`)
- Fastfind (`Sedgewick`)
- MFSET initial/merge/find (`Aho`)
- MERGE and SPLIT set algorithms (`Aho`)
- Set union/intersection (`Aho`)

## Graph Algorithms

- Breadth-first tree traversal (`DSA`)
- Graph traversal / priority-first search (`Sedgewick`)
- Sparse priority-first search (`Sedgewick`)
- Dense priority-first search (`Sedgewick`)
- Cycle testing (`Sedgewick`)
- Shortest path printing (`Aho`)
- Euclidean minimum spanning tree (`Sedgewick`)
- Ford-Fulkerson maximum flow (`Sedgewick`)
- Max-flow/min-cut (`Sedgewick`)
- Bipartite matching (`Aho`)
- Maximal matching (`Aho`)
- Maximum matching (`Sedgewick`)
- Weighted matching (`Sedgewick`)
- Augmenting path matching (`Aho`)
- Stable marriage (`Sedgewick`)
- M-coloring backtracking (`DAA Notes`)
- Exhaustive graph traversal for cycles (`Sedgewick`)

## String / Parsing / Compression / Crypto

- Brute-force string search (`Sedgewick`)
- Knuth-Morris-Pratt string search (`Sedgewick`)
- Boyer-Moore string search (`Sedgewick`)
- Mismatched-character Boyer-Moore variant (`Sedgewick`)
- Rabin-Karp string search (`Sedgewick`)
- Multiple string searches (`Sedgewick`)
- Finite-state-machine pattern matching (`Sedgewick`)
- Nondeterministic pattern matching machine simulation (`Sedgewick`)
- Regular-expression pattern matching (`Sedgewick`)
- Bottom-up parsing (`Sedgewick`)
- Shift-reduce parsing (`Aho`)
- Top-down compiler expression/term/factor (`Sedgewick`)
- Run-length encoding (`Sedgewick`)
- Variable-length encoding (`Sedgewick`)
- Caesar cipher (`Sedgewick`)
- Vigenere cipher (`Sedgewick`)
- Vernam cipher (`Sedgewick`)
- Product cipher (`Sedgewick`)
- Public-key cryptosystem (`Sedgewick`)

## Computational Geometry

- Line drawing / scan conversion (`Sedgewick`)
- Line intersection (`Sedgewick`)
- Horizontal-vertical line intersection (`Sedgewick`)
- General line intersection (`Sedgewick`)
- Rectangle intersection (`Sedgewick`)
- Circle intersection (`Sedgewick`)
- Point-in-rectangle test (`Sedgewick`)
- Point-in-polygon test (`Sedgewick`)
- Simple closed path (`Sedgewick`)
- Convex hull (`Sedgewick`)
- Package wrapping / Jarvis march (`Sedgewick`)
- Graham scan (`Sedgewick`)
- Floyd-Eddy convex hull method (`Sedgewick`)
- Hull selection (`Sedgewick`)
- Closest pair (`Sedgewick`)
- All-nearest-neighbors (`Sedgewick`)
- Voronoi diagram (`Sedgewick`)
- Range searching (`Sedgewick`)
- One-dimensional range search (`Sedgewick`)
- Grid method range searching (`Sedgewick`)
- 2D-tree insertion/range search (`Sedgewick`)
- Multidimensional range searching (`Sedgewick`)
- Projection method (`Sedgewick`)

## Design / Optimization / NP

- FIFO branch and bound (`DAA Notes`)
- LIFO branch and bound (`DAA Notes`)
- LC / least-cost branch and bound (`DAA Notes`)
- Local search (`Aho`)
- 2-opt for TSP (`Aho`)
- Exhaustive search (`Sedgewick`)
- Approximation algorithms (`Sedgewick`)
- Knapsack DP (`DAA Notes`)
- Greedy knapsack (`DAA Notes`)
- Fractional knapsack (`DAA Notes`)
- Bounded knapsack (`DAA Notes`)
- Unbounded knapsack (`DAA Notes`)
- Branch-and-bound knapsack (`DAA Notes`)
- Nondeterministic knapsack / DKP (`DAA Notes`)
- Job sequencing with deadlines (`DAA Notes`)
- Matrix-chain multiplication (`DAA Notes`)
- Reliability design (`DAA Notes`)
- Multistage graph forward algorithm Fgraph (`DAA Notes`)
- Multistage graph backward algorithm Bgraph (`DAA Notes`)
- Graph coloring (`DAA Notes`)
- Cook's theorem / satisfiability NP-completeness (`Sedgewick`)

## Advanced / Parallel / Memory

- Fast Fourier Transform (`Sedgewick`)
- Odd-even merge (`Sedgewick`)
- Bitonic merge (`Sedgewick`)
- Perfect shuffle (`Sedgewick`)
- Systolic arrays (`Sedgewick`)
- Divide-and-conquer integer multiplication (`Aho`)
- Round-robin tournament construction (`Aho`)
- Towers of Hanoi (`Aho`)
- Minimal-cost triangulation (`Aho`)
- Simplex method (`Sedgewick`)
- Bland's anti-cycling method (`Sedgewick`)
- Pivoting in simplex (`Sedgewick`)
- Steepest descent method (`Sedgewick`)
- Garbage collection marking algorithm (`Aho`)
- Nonrecursive marking algorithm (`Aho`)
- Storage compaction / forwarding-address computation (`Aho`)
- Buddy systems / Fibonacci buddy system (`Aho`)
- Merging adjacent free memory blocks (`Aho`)
